doctype
html
    title #{title}

    header
        link(rel='stylesheet' href='//cdn.datatables.net/1.10.3/css/jquery.dataTables.css')

        script(src='http://ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js')
        script(src='//cdn.datatables.net/1.10.3/js/jquery.dataTables.js')

    body   
        h2 Search results
        - if (res.length > 0)
            table.obj(id="table_id")
                thead
                    tr
                        for key in Object.keys(res[0])
                            th #{key}
                        th= ""    
                tbody
                    for item in res
                        tr
                            for value in item
                                td= value
                            var event_id = item.event_id
                            td
                             button#btnPurchase(type='button',onClick="")  event_id  


            script
                $('#table_id').DataTable(); 
        - else 
            p No results found